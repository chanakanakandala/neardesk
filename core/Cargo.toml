[package]
name = "neardesk-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "neardesk_core"
path = "src/lib.rs"

[dependencies]
# Cross-platform local interface enumeration (replaces per-OS shell-outs).
if-addrs = "0.13"
